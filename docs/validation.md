# 配布準備時の確認（2026-09-09）

- npm ci：成功。
- 操作音・BGM生成：成功。48秒、合成ピーク約0.535。
- Remotionバンドル：成功。
- 静止画の再レンダリング：実行環境のネットワークインターフェース取得で停止（uv_interface_addresses）。画像出力と全編再レンダリングはこの環境では未確認。
- 制作時の元動画の検証は chikaidev-video-analysis.md に記録。
- 配布準備で変更したのは起動手順、出力フォルダ作成、ブラウザ指定、ドキュメント類。映像UIのソースは元のv2と同一。

## バージョン履歴

保存済み初版ZIPを復元し、v1を別コミットとして公開しました。保存済みZIPの現行版はv2へ戻しています。

- v1のsrc/index.tsx、render.cjs、audio.py、人物MP4は復元元の原本と一致。
- v1の操作音生成とrender.cjsの構文確認は成功。
- v1は配布時に参考元の切り出し画像、生成WAV、検証出力を除外。READMEの補足、ISCライセンス、.gitignoreを追加。
- v2の実装ツリーは先行公開版を引き継ぎ、v1を親とする独立コミットを作成。
- 既存mainの履歴は保持し、v1→v2の履歴を取り込んでいます。
- v1/v2の全編再レンダリングは今回未実施です。
